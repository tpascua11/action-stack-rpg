================================================================================
  DAQ-GAME TODO LIST
================================================================================

[x] Create a centralized image/asset index file (e.g. src/asssets/index.js) that
    exports all image imports in one place, so components don't each need their
    own import statements for icons, status images, portraits, etc.

[ ] Consolidate enemy and player tag pools into one TagPool component.
    Add a `compact` prop that switches to single-column, smaller sizing for
    enemies. EnemyZone should use <TagPool tags={tags} compact /> instead of
    maintaining its own duplicate tag row logic.

[ ] Install and integrate `react-sprite-animator` for sprite sheet animations on cards.
    Use <SpriteAnimator sprite={card.spriteSheet} frameCount={8} fps={12} /> in card components.

[ ] Install and integrate `use-sound` (Howler wrapper) for sound effects.
    React hook API: const [play] = useSound('/sounds/sfx.mp3') — use on attacks, hits, etc.

[ ] Split App.jsx responsibilities:
    - Move battleReducer + case handlers to src/battle/reducer.js
    - Move buildInitialState + buildEnemyQueue to src/battle/initialState.js
    - Move all battle JSX into src/components/BattleScreen.jsx
    - Make App.jsx a pure phase-based screen switcher (switch on state.phase)

[ ] Add pre-battle RPG screen phases before QUEUE_SETUP:
    TITLE → CLASS_SELECT → MAP → QUEUE_SETUP → BATTLE → RESULT
    - TitleScreen: logo + start button, dispatches START_GAME
    - ClassSelectScreen: pick a class from CLASS_REGISTRY, feeds player.class into state
    - MapScreen: pick an encounter node, feeds enemy list into buildInitialState

[ ] Set up React Context for persistent player data (separate from battle state):
    - Player progression, level, experience
    - Inventory and equipment
    - Unlocked cards and deck building
    - Stat upgrades between fights
    Split into two state concerns: GameContext (persists across battles) and
    battle state (resets each fight). GameContext wraps all screens so any
    component can access player data without prop drilling.

================================================================================
